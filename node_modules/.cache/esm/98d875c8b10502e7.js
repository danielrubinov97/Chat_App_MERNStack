let gql;_b10‍.x([["default",()=>_b10‍.o]]);_b10‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);

_b10‍.d(gql`
    extend type Query {
        me: User @auth
        user(id: ID!): User @auth
        users: [User!]! @auth
    }

    extend type Mutation {
        signUp(email: String!, username: String!, name: String!, password: String!): User @guest
        signIn(email: String!, password: String!): User @guest
        signOut: Boolean @auth
    }

    type User {
        id: ID!
        email: String!
        username: String!
        name: String!
        createdAt: String!
    }
`);
